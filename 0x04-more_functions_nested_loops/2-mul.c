#include "main.h"
#include <stdio.h>

/**
 * mul - multiply two numbers
 * @a: number to be multplied
 * @b: second number to be multiplied
 * Return: result
 */
int mul(int a, int b)
{
	return (a * b);
}
