#include <stdio.h>

/**
 * main - main point
 *
 * Description: description here
 * Return: success (0)
 */
int main(void)
{
	char ch;

	ch = 'z';
	while (ch >= 'a')
		putchar(ch);
		ch--;
	putchar('\n');
	return (0)
}
