#include "main.h"
#include <stdlib.h>

/**
 * _abs - computes the absolute value
 *
 * @c: the number to be checked
 *
 * Return: absolute value
 */
int _abs(int c)
{
	int result = abs(c);

	return (result);
}
